name: Context Examples

on: [push]

jobs:
  my-context:
    runs-on: ubuntu-latest
    steps:
      - name: "My step"
        run: |
          echo "Action: ${{ github.action }}"
          echo "Actor: ${{ github.actor }}"
